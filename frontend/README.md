# TripMap frontend

Vue 3 + TypeScript + Vite. See the **Frontend** and **Pre-commit hooks** sections in the repo-root `CLAUDE.md` for the full dev workflow.

```sh
npm install
npm run dev      # http://localhost:5173
npm run build    # type-check + production build
npm run lint     # eslint + prettier --check
```
